# Instructions Sheet

The areas of shapes calculator requires a third-party library to display
correctly. Install it by typing in the following code inside of a command-line
interface.


## Windows

```
python -m pip install pyfiglet
```

## Linux

```
python3 -m pip install pyfiglet
```